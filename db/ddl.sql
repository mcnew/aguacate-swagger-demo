CREATE TABLE `cenicienta`.`color` (
	  `id` int(11) NOT NULL AUTO_INCREMENT,
	  `name` varchar(50),
	  `description` varchar(100),
	  `date` date,
	  `startTime` time,
	  `endTime` time,
	  PRIMARY KEY (`id`)
);

